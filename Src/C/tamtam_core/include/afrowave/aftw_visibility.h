#pragma once

/* Afrowave visibility boundary (marker header).

   This header intentionally defines no symbols.
   It exists to enforce a project rule:

   - No payload interpretation symbols are introduced at this layer.
   - tamtam_core remains payload-agnostic by design.
*/
